# Scenario Tests

Runtime scenario scripts will live here once the gameplay coordinator exposes the `begin_test_mode()`, `apply_bot_action()`, and `get_debug_snapshot()` hooks documented in `docs/ARCHITECTURE.md`.

Until then, the canonical scenario data lives in `res://data/scenarios/` and is catalog-validated by the diagnostics harness.
